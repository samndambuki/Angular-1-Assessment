import { FilterByFeesBalancePipe } from './filter-by-fees-balance.pipe';

describe('FilterByFeesBalancePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterByFeesBalancePipe();
    expect(pipe).toBeTruthy();
  });
});
