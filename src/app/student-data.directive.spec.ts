import { StudentDataDirective } from './student-data.directive';

describe('StudentDataDirective', () => {
  it('should create an instance', () => {
    const directive = new StudentDataDirective();
    expect(directive).toBeTruthy();
  });
});
